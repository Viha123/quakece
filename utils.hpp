// things i want here
// square num to rank file
// rank file to square num
#pragma once
namespace utils {
  int getNumFromRF(int rank, int file);
  int getRank(int num);
  int getFile(int num);
  int getYPos(int row);
  int getXPos(int col);
}